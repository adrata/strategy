/**
 * 📝 Shared Types
 * Global type declarations and interfaces
 */

// Re-export type declarations (they're in .d.ts files)
// This ensures the types directory is recognized as a module

// Export a placeholder to make this a proper module
export {};
