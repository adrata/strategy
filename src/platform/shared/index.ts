/**
 * 📚 Shared Library
 * Cross-platform utilities, hooks, types, and shared concerns
 * No UI components - only logic and utilities
 */

// Shared Hooks are in /platform/hooks (not in shared folder)

// Type Definitions (global types and interfaces)
export * from "./types";

// Shared UI Utilities (layout helpers, not actual components)
export * from "./components/ui";
export * from "./components/layout";

// Note: Actual UI components are in /ui
// Feature-specific components are in /components
