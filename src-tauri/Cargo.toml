[package]
name = "adrata-tauri"
version = "1.0.2"
edition = "2021"

[lib]
name = "adrata_tauri"
crate-type = ["rlib"]

# Security: Force newer glib version to fix VariantStrIter unsoundness vulnerability
# This overrides the transitive 0.18.5 dependency
[dependencies]
glib = "0.21"

